plugins {
    id 'java'
    id 'maven-publish'
    id 'com.github.johnrengelman.shadow' version '7.1.2' apply false
}

repositories { // Added repositories block here
    mavenCentral()
    maven { url 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/' }
    maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
    maven { url 'https://repo.codemc.org/repository/maven-public/' }
    maven { url 'https://jitpack.io' }
    maven { url 'https://repo.papermc.io/repository/maven-public/' }
}

allprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'
}

subprojects {
    apply plugin: 'com.github.johnrengelman.shadow'

    repositories {
        mavenCentral()
        maven { url 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/' }
        maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
        maven { url 'https://repo.codemc.org/repository/maven-public/' }
        maven { url 'https://jitpack.io' }
        maven { url 'https://repo.papermc.io/repository/maven-public/' }
    }

    dependencies {
        if (project.name != 'colorapi') {
            implementation 'com.github.CryptoMorin:XSeries:9.8.0'
            compileOnly "io.papermc.paper:paper-api:1.21.4-R0.1-SNAPSHOT" // Changed this line
            compileOnly 'com.github.MilkBowl:VaultAPI:1.7'
        }
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        options.release = 21
    }
}

dependencies {
    subprojects.each { implementation(it) }
    compileOnly "io.papermc.paper:paper-api:1.21.4-R0.1-SNAPSHOT"
}



publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.kbskyblock'
            artifactId = 'KBCore'
            version = version
        }
    }
}

task runServer(type: JavaExec) {
    dependsOn build
    group = "Execution"
    description = "Runs the shadowJar on a test server"
    classpath = sourceSets.main.runtimeClasspath
    mainClass = "org.bukkit.craftbukkit.Main"
    args 'nogui'
    workingDir = file('run')

    doFirst {
        mkdir 'run'
        copy {
            from configurations.runtimeClasspath
            into 'run/plugins'
        }
    }
}
