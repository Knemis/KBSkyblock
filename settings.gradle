// KBSkyblock/settings.gradle

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'KBSkyblock'

include(
        'core',
        'colorapi',
        'teams'
)